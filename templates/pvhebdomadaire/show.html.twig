{% extends 'base.html.twig' %}

{% block title %}Détails du PV{% endblock %}

{% block body %}
          <center class="titre">Détails du PV</center>
    <br> 


 <div class="container">

    <table class="table">
        <tbody>
            <tr>
                <th>Intitulé du PV</th>
                <td>{{ pvhebdomadaire.label }}</td>
            </tr>
             <tr>
                <th>Nom du projet</th>
                <td>{{ pvhebdomadaire.nomprojet }}</td>
            </tr>
            <tr>
            <tr>
                <th>Document</th>
                <td><a href="{{ asset('uploads/brochures/' ~ pvhebdomadaire.brochureFilename) }}" target="_blank">Voir le fichier (PDF)</a></td>
            </tr>
        </tbody>
    </table>

    
     <div class="text-center"> 
        <a href="{{ path('pvhebdomadaire_index') }}" class="btn btn-info">Liste des PV hebdomadaire</a>

        <a href="{{ path('pvhebdomadaire_edit', {'id': pvhebdomadaire.id}) }}"class="btn btn-warning">Modifier</a>
    </div>
     {{ include('pvhebdomadaire/_delete_form.html.twig') }}
</div>
{% endblock %}
