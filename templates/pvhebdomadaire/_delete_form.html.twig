<form method="post" action="{{ path('pvhebdomadaire_delete', {'id': pvhebdomadaire.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pvhebdomadaire.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
