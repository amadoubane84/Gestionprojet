{% extends 'base.html.twig' %}

{% block title %}Telechargement{% endblock %}

{% block body %}
    <h1>Telechargement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ telechargement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ telechargement.Nom }}</td>
            </tr>
            <tr>
                <th>BrochureFileName</th>
                <td>{{ telechargement.brochureFileName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('telechargement_index') }}">back to list</a>

    <a href="{{ path('telechargement_edit', {'id': telechargement.id}) }}">edit</a>

    {{ include('telechargement/_delete_form.html.twig') }}
    <footer>
<span class="badge badge-pill badge-dark"><a href="{{ path('etude') }}">Suivi et Contrôle</a></span>
    </footer>
{% endblock %}
