{% extends 'base.html.twig' %}

{% block title %}Liste des rapports trimestriels{% endblock %}

{% block body %}
 <center class="titre">Liste des rapports trimestriels</center>
 <br>  
  
 <div class="container">
  <div class="text-center">

    <table class="table" id="myTable">
        <thead>
            <tr id="header">
                <th>Intitulé du rapport</th>
             <th>Nom du projet</th>
                <th>Document</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rtrimestriel in rtrimestriels %}
            <tr class="ttttt">
                <td>{{ rtrimestriel.label }}</td>
                <td>{{ rtrimestriel.nomprojet }}</td>

                <td>
                <a href="{{ asset('uploads/brochures/' ~ rtrimestriel.brochureFilename) }}" target="_blank">Voir le fichier (PDF)</a>
                </td>
                <td>
                    <a href="{{ path('rtrimestriel_show', {'id': rtrimestriel.id}) }}">Voir</a>
                    <a href="{{ path('rtrimestriel_edit', {'id': rtrimestriel.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun rapport enregistré</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</br></br>
    <a href="{{ path('rtrimestriel_new') }}" class="btn btn-success">Ajouter un rapport</a>
     </div>
    </div>
{% endblock %}
