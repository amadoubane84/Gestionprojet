{{ form_start(form) }}
   
    <div class="container formulaire" >
      <style>
   .milieu{
      
       margin-left:350px;
   }
    </style>
<br> 
    <div class="text-center">
   <div class="milieu"> 
        <div class="col-md-6">
            {{ form_row(form.nomprojet, {'label':'Sélectionner le projet'}) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.Visite,{'label':'Fiche de visite N°(1__9)'}) }}
        </div>  
        <div class="col-md-6">
            {{ form_row(form.brochure, {'label':'Sélectionner le fichier'}) }}
        </div>
        <div class="col-md-6"  id="etat">
        {{ form_row(form.etat,{
                          'label':'État de la demande'
                             }) }}</div> <br> 
    </div>
        <div class="text-center">
        <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
        </div> <br> 
</div>
{{ form_end(form) }}
