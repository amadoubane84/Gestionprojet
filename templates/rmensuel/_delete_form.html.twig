<form method="post" action="{{ path('rmensuel_delete', {'id': rmensuel.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rmensuel.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
