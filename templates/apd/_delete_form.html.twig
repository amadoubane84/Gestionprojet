<form method="post" action="{{ path('apd_delete', {'id': a_p_d.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ a_p_d.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
