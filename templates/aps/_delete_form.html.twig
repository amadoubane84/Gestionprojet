<form method="post" action="{{ path('aps_delete', {'id': a_p.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ a_p.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
