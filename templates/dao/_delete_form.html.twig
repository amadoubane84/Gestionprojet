<form method="post" action="{{ path('dao_delete', {'id': d_a_o.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ d_a_o.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
